import LogoLight from "@components/Logo/LogoLight";

const AuthHeader = () => {
  return (
    <div className="py-8 px-page-layout">
      <header>
        <a href="#">
          <LogoLight />
        </a>
      </header>
    </div>
  );
};

export default AuthHeader;
